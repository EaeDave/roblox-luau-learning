local function variaveis()
    local ReplicatedStorage = game:GetService("ReplicatedStorage")

    local part = workspace.Part -- Variável local referente ao objeto Part no workspace

    local mensagem = 'Olá, Mundo'  -- Armazenando uma string em uma variável
    print(mensagem)  -- Output da mensagem no console

    part.Color = Color3.fromRGB(255, 000, 000)
    end

-- variaveis()